{
  "name": "@nordenwinds/functions",
  "description": "Functions for officialsite",
  "author": "Norden Wind Ensemble",
  "license": "MIT",
  "type": "module",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "serve": "pnpm run build && wrangler dev ./dist/index.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.3",
    "@hono/swagger-ui": "^0.5.1",
    "@hono/zod-openapi": "^1.0.0",
    "@slack/types": "^2.14.0",
    "@slack/webhook": "^7.0.4",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@microlabs/otel-cf-workers": "^1.0.0-rc.49",
    "hono": "^4.6.18",
    "uuid": "^13.0.0",
    "zod": "^4.0.0"
  }
}
